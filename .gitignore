# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules

dist

# next.js build output
apps/frontend/.next

# local dev uploads (Docker uses the uploads_data volume instead)
apps/backend/uploads

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.*
!.env.example

# Migration/populator scripts (contain data, run manually)
apps/backend/scripts/

# Docker development environment overrides
docker-compose.override.yml